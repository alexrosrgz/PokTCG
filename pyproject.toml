[project]
name = "poktcg"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]
web = ["fastapi", "uvicorn"]

[tool.pytest.ini_options]
testpaths = ["tests"]
